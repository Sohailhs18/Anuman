{
  "$schema": "https://raw.githubusercontent.com/firebase/firebase-tools/master/schema/firestore-blueprint.json",
  "entities": {
    "appointments": {
      "title": "Appointments",
      "description": "Patient and client care requests, equipment bookings, and clinical inquiries in Patna",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "createdAt": { "type": "number" },
        "timestamp": { "type": "string" },
        "name": { "type": "string" },
        "phone": { "type": "string" },
        "service": { "type": "string" },
        "preferredDate": { "type": "string" },
        "preferredTime": { "type": "string" },
        "address": { "type": "string" },
        "notes": { "type": "string" },
        "staffNotes": { "type": "string" },
        "status": { "type": "string" },
        "source": { "type": "string" }
      },
      "required": ["id", "name", "phone", "createdAt"]
    },
    "careerApplications": {
      "title": "CareerApplications",
      "description": "Job and caregiver applications submitted by nurses, GDAs, and physiotherapists in Patna",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "createdAt": { "type": "number" },
        "timestamp": { "type": "string" },
        "name": { "type": "string" },
        "phone": { "type": "string" },
        "email": { "type": "string" },
        "role": { "type": "string" },
        "qualification": { "type": "string" },
        "experience": { "type": "string" },
        "preferredShift": { "type": "string" },
        "preferredLocality": { "type": "string" },
        "about": { "type": "string" },
        "status": { "type": "string" }
      },
      "required": ["id", "name", "phone", "role", "createdAt"]
    }
  },
  "firestoreRules": "rules_version = '2';\nservice cloud.firestore {\n  match /databases/{database}/documents {\n    match /appointments/{appointmentId} {\n      allow create: if request.resource.data.name is string && request.resource.data.phone is string;\n      allow read, update, delete: if request.auth != null;\n    }\n    match /careerApplications/{applicationId} {\n      allow create: if request.resource.data.name is string && request.resource.data.phone is string;\n      allow read, update, delete: if request.auth != null;\n    }\n  }\n}"
}
